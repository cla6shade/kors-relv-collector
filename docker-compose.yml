services:
  postgres:
    image: postgres:18
    container_name: kors-relv-postgres
    restart: unless-stopped
    env_file:
      - .env.docker
    ports:
      - "5432:5432"
    volumes:
      - ./db:/var/lib/postgresql
